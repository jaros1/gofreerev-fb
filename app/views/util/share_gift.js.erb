<% if @api_gift -%>
(function () {
    var provider = '<%= @api_gift.provider %>' ;
    var gift_id = '<%= @api_gift.gift.id %>' ;
    var link = '<%= @api_gift.deep_link %>' ;
    var max_lng = <%= @max_lng %> ;
    var extra = '<%= @extra %>' ; // provider specific parameters
    share_gift(provider, gift_id, link, max_lng, extra) ;
})();
<% end -%>