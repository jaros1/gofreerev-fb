<%# post processing in my.js / post_ajax_add_older_comments_handler - move older comments up before first comment %>
<% if @api_comments -%>
$('#gifts').append('<%= j render("api_comments3", :api_comments3 => @api_comments) %>');
post_ajax_add_older_comments(<%= @gift.id %>, <%= @first_comment_id %>);
<% end -%>
<% if @errors2.size > 0 %>
<%# error response - inject error into tasks_errors2 table and move error to placeholder table within gifts/index page -%>
<% logger.debug2 "error response - @errors2 = #{@errors2}" -%>
$('#tasks_errors2').append('<%= j render('shared/ajax_errors2') %>');
move_tasks_errors2();
<% end %>