<% if @comment.errors.count == 0 %>
<% puts  "id = .gift-#{@comment.gift.id}-comments" -%>
<% puts  "html = " + render("comment", :comment => @comment) -%>
<% puts  "js = " + j(render("comment", :comment => @comment)) -%>
debugger;
$('#gift-<%= @comment.gift.id %>-comments').append('<%= j render("comment", :comment => @comment) %>');

<% else %>

$('.new_comment').remove();
$('#new_comment_link').after('<%= j render("form") %>');

<% end %>