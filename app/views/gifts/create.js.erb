<% puts "@errors.size = #{@errors.size}, @gifts.size = #{@gifts.size}"
   raise "empty response from gifts/create" if @errors.size == 0 and @gifts.size == 0
-%>
<% if @errors.size > 0 -%>
$('#tasks_errors').append('<%= j render("shared/ajax_errors") %>');
ajax_flash_new_table_rows('tasks_errors',<%= @errors.size %>);
<% end -%>
<% if @gifts.size > 0 -%>
document.getElementById('new_messages_buffer_div').innerHTML = '<%= j render("create") %>';
insert_update_gifts(100) ; <%# move new gift from buffer to gifts table -%>
<% end -%>
